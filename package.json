{
  "name": "onestsignt-local",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:api": "bun run --cwd node-api dev",
    "dev:web": "bun run --cwd web dev",
    "check:api": "bun run --cwd node-api check",
    "build:api": "bun run --cwd node-api build",
    "check:web": "bun run --cwd web check",
    "build:web": "bun run --cwd web build"
  }
}
